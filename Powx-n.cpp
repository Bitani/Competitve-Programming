// Time complexity of O(1).

class Solution {
public:
    ////x is our number and n is the raised(power) number which means x raised to the power n.
    double myPow(double x, int n)
    {
      return pow(x, n);
                 // return (double)pow(x, n);
    }
};
